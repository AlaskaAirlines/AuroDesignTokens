/**
 * Build Constants
 */

export const PATHS = {
  DIST: './dist',
  SRC: './src'
};

export const CSS = {
  TARGET_FILE_PATTERN: 'CSSCustomProperties',
  BUNDLED_FILE_NAME: 'CSSCustomProperties--bundled.css',
  ROOT_SELECTOR: ':root, :root([onLight])',
  ROOT_SELECTOR_ONDARK: ':root([onDark])'
};
